pipeline {
    agent any

    stages {
        stage('parameterised') {
            steps {
                echo "hello from learning of webhook......should be success..done"
                echo "hello "
            }
        }
    }
}
